<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= business_name %> Invoice
    </title>
    <style>
        /* --- A4 SETTINGS --- */
        @page {
            size: A4;
            margin: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #E0E0E0;
            font-size: 12px;
        }

        .invoice-box {
            /* Force the box to be exactly A4 height */
            height: 297mm;
            width: 210mm;
            margin: 0 auto;
            padding: 30px;
            background-color: #fff;

            /* FLEXBOX: Organizes content vertically */
            display: flex;
            flex-direction: column;
            justify-content: flex-start;

            box-sizing: border-box;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.15);
        }

        @media print {
            body {
                background-color: #fff;
            }

            .invoice-box {
                width: 100%;
                height: 297mm;
                margin: 0;
                padding: 30px;
                box-shadow: none;
            }
        }

        /* Prevent sections from shrinking */
        .header, .footer, .top-info, .customer-section, .bottom-wrapper, h2 {
            flex-shrink: 0;
        }

        /* --- HEADER & INFO --- */
        .header {
            text-align: center;
            margin-bottom: 10px;
        }

        .logo {
            width: 120px;
            height: auto;
            display: block;
            margin: 0 auto 10px auto;
        }

        .company-name {
            font-size: 24px;
            font-weight: bold;
            color: #000;
            text-transform: uppercase;
            margin: 0;
        }

        .tagline {
            font-size: 14px;
            font-style: normal;
            margin-top: 5px;
        }

        .footer {
            margin-bottom: 20px;
            font-size: 11px;
        }

        .bottom-address {
            text-align: center;
            padding-top: 5px;
        }

        .bottom-address p {
            margin: 3px 0;
        }

        .top-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            padding: 10px 0;
        }

        .info-column {
            width: 48%;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
        }

        .info-label {
            font-weight: bold;
            white-space: nowrap;
        }

        .top-info .info-row span:last-child {
            text-align: right;
            flex-grow: 1;
        }

        /* --- UPDATED CUSTOMER SECTION --- */
        .customer-section {
            margin-bottom: 20px;
            border: 1px solid #000;
            padding: 10px;
            
            /* Create two distinct columns */
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        /* Left Column (Attn, Customer) */
        .cust-col-left {
            flex-grow: 1; /* Allow it to fill space */
            margin-right: 20px; /* Space between columns */
            display: flex;
            flex-direction: column;
        }

        .cust-col-left .info-row {
            display: flex;
            justify-content: flex-start;
            margin-bottom: 5px;
        }
        
        .cust-col-left .info-label {
            width: 110px;
            min-width: 110px;
            display: inline-block;
        }
        
        .cust-col-left .info-row span:last-child {
            text-align: left;
        }

        /* Right Column (GST, NTN, PO) */
        .cust-col-right {
            /* Auto width makes it shrink-wrap the content */
            width: auto;
            flex-shrink: 0; /* Prevent it from shrinking */
            display: flex;
            flex-direction: column;
            /* Align rows to the LEFT of this block to create a straight line */
            align-items: flex-start; 
        }

        .cust-col-right .info-row {
            display: flex;
            justify-content: flex-start; /* Label and Value sit next to each other */
            margin-bottom: 5px;
        }

        .cust-col-right .info-label {
            width: 80px; 
            min-width: 80px;
            text-align: left; /* Align the headings vertically */
            margin-right: 10px;
        }

        .cust-col-right .info-row span:last-child {
            text-align: left;
        }

        /* --- TABLE --- */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            flex-grow: 1; /* Grow to fill space */
            height: auto; 
        }

        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: center;
            vertical-align: top;
        }

        /* UPDATED: Center Description Heading, Left Align Content */
        th:nth-child(2) {
            text-align: center;
        }
        td:nth-child(2) {
            text-align: left;
        }

        th {
            background-color: #f9f9f9;
            height: 40px; 
        }

        tr {
            height: 1px;
        }
        
        td {
            height: auto;
        }

        /* --- LAYOUT WRAPPER --- */
        .bottom-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-top: 10px;
            page-break-inside: avoid;
        }

        .left-section {
            width: 45%;
            font-size: 11px;
        }

        .right-section {
            width: 50%;
        }

        /* Terms & Totals */
        .terms ul {
            padding-left: 20px;
            margin: 5px 0;
        }

        .terms li {
            margin-bottom: 5px;
        }

        .totals-table {
            width: 100%;
            border-collapse: collapse;
            height: auto;
        }

        .totals-table td {
            text-align: right;
            border: 1px solid #000;
        }

        .totals-table .label {
            font-weight: bold;
            text-align: left;
        }

        .signature-section {
            display: flex;
            justify-content: flex-start;
            margin-top: 20px;
            page-break-inside: avoid;
        }

        .signature-box {
            text-align: center;
            width: 200px;
            border-top: none;
            padding-top: 10px;
            position: relative;
        }

        .signature-box img {
            display: block;
            margin: 0 auto;
            object-fit: contain;
        }

        .signature-box img:nth-of-type(1) {
            width: 90px;
            margin-bottom: -50px;
            position: relative;
            z-index: 1;
            opacity: 0.9;
        }

        .signature-box img:nth-of-type(2) {
            width: 110px;
            position: relative;
            z-index: 2;
            margin-bottom: 5px;
        }
    </style>
</head>

<body>

    <div class="invoice-box">
        <div>
            <div class="header">
                <img src="<%= logo_url %>" alt="<%= business_name %> Logo" class="logo">
                <h1 class="company-name"><%= business_name %></h1>
                <div class="tagline"><%= slogan %></div>
            </div>

            <div class="footer">
                <div class="bottom-address">
                    <p><%= landmark %>, <%= street %>, <%= area %>, <%= city %>, <%= country %></p>
                    <p><%= tele_no %>, <%= email %>, <%= website %></p>
                </div>
            </div>

            <h2 style="text-align: center; text-decoration: underline; margin: 10px 0;">Sales Tax Invoice</h2>

            <div class="top-info">
                <div class="info-column">
                    <div class="info-row"><span class="info-label">Tele. No.:</span> <span><%= tele_no %></span></div>
                    <div class="info-row"><span class="info-label">GST No.:</span> <span><%= your_GST %></span></div>
                    <div class="info-row"><span class="info-label">NTN:</span> <span><%= your_NTN %></span></div>
                </div>
                <div class="info-column">
                    <div class="info-row"><span class="info-label">Invoice #:</span> <span><%= invoice_no %></span></div>
                    <div class="info-row"><span class="info-label">Date:</span> <span><%= date %></span></div>
                    <div class="info-row"><span class="info-label">HS Code:</span> <span><%= hs_code %></span></div>
                </div>
            </div>

            <div class="customer-section">
                <div class="cust-col-left">
                    <div class="info-row">
                        <span class="info-label">Attn To:</span> 
                        <span><%= Attn_to %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Customer:</span>
                        <span>
                            <strong><%= customer_name %></strong><br>
                            <%= customer_landmark %>, <%= customer_street %>, <%= customer_area %><br>
                            <%= customer_city %>, <%= customer_country %>
                        </span>
                    </div>
                </div>

                <div class="cust-col-right">
                    <div class="info-row">
                        <span class="info-label">GST No.:</span> 
                        <span><%= customer_GST %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">NTN:</span> 
                        <span><%= customer_NTN %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">PO:</span> 
                        <span><%= customer_PO %></span>
                    </div>
                </div>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th style="width: 5%;">Sr #</th>
                    <th style="width: 40%;">Description</th>
                    <th style="width: 10%;">Range</th>
                    <th style="width: 10%;">Design</th>
                    <th style="width: 10%;">Qty</th>
                    <th style="width: 10%;">Rate</th>
                    <th style="width: 15%;">Amount</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach((item, index)=> { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= item.name %></td>
                        <td><%= item.range || "Pure" %></td>
                        <td><%= item.design || "As Approved" %></td>
                        <td><%= item.quantity %></td>
                        <td><%= item.price %></td>
                        <td><%= (item.quantity * item.price).toFixed(2) %></td>
                    </tr>
                <% }); %>
                <tr>
                    <td colspan="7" style="height: 100%;"></td>
                </tr>
            </tbody>
        </table>

        <div class="bottom-wrapper">
            <div class="left-section">
                <div class="terms">
                    <strong>Terms & Conditions:</strong>
                    <ul>
                        <li>Goods once delivered shall not be taken back.</li>
                        <li>Company will not be responsible for goods damage after Delivery.</li>
                        <li>Without prejudice to the recovery measures, surcharge @ 1% per week shall be levied.</li>
                    </ul>
                </div>

                <div class="signature-section">
                    <div class="signature-box">
                        <img src="<%= stamp_url %>" alt="<%= business_name %> stamp" />
                        <img src="<%= signature_url %>" alt="<%= business_name %> signature" />
                        For <%= business_name %>
                    </div>
                </div>
            </div>

            <div class="right-section">
                <table class="totals-table">
                    <tr>
                        <td class="label">Sub Total</td>
                        <td><%= sub_total %></td>
                    </tr>
                    <tr>
                        <td class="label">Discount</td>
                        <td><%= discount %></td>
                    </tr>
                    <tr>
                        <td class="label">Taxable Value</td>
                        <td><%= taxable_value %></td>
                    </tr>
                    <tr>
                        <td class="label">Sales Tax @ <%= sales_tax_rate %>%</td>
                        <td><%= sales_tax %></td>
                    </tr>
                    <tr>
                        <td class="label">Special Excise Duty @ <%= special_excise_rate %>%</td>
                        <td><%= special_excise_tax %></td>
                    </tr>
                    <tr>
                        <td class="label">Add: Freight/Other Charges</td>
                        <td><%= freight_other_charges %></td>
                    </tr>
                    <tr>
                        <td class="label" style="font-size: 14px;">Value Including Sales Tax</td>
                        <td style="font-weight: bold; font-size: 14px;"><%= value_including_sales_tax %></td>
                    </tr>
                </table>

                <div style="margin-top: 10px;">
                    <strong>Amount in Words:</strong> <span><%= amount_in_words %></span>
                </div>
            </div>
        </div>

    </div>

</body>

</html>