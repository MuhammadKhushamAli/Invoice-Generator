<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delivery Challan - <%= business_name %>
    </title>
    <style>
        /* --- A4 SETTINGS --- */
        @page {
            size: A4;
            margin: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #E0E0E0;
            font-size: 12px;
            color: #000;
        }

        .invoice-box {
            height: 297mm;
            width: 210mm;
            margin: 0 auto;
            padding: 30px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.15);
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
        }

        @media print {
            body {
                background-color: #fff;
            }

            .invoice-box {
                width: 100%;
                height: 297mm;
                margin: 0;
                padding: 30px;
                box-shadow: none;
            }
        }

        .header,
        .footer,
        .top-info,
        .customer-section,
        .bottom-wrapper {
            flex-shrink: 0;
        }

        /* --- HEADER --- */
        .header {
            text-align: center;
            margin-bottom: 5px;
        }

        .logo {
            width: 120px;
            height: auto;
            display: block;
            margin: 0 auto 10px auto;
        }

        .company-name {
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            margin: 0;
        }

        .footer {
            margin-bottom: 20px;
            font-size: 11px;
        }

        .bottom-address {
            text-align: center;
            padding-top: 5px;
        }

        .bottom-address p {
            margin: 3px 0;
        }

        /* --- TOP INFO GRID --- */
        .top-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            padding: 10px 0;
        }

        .info-column {
            width: 48%;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
        }

        .info-label {
            font-weight: bold;
        }

        /* --- CUSTOMER SECTION --- */
        .customer-section {
            margin-bottom: 20px;
            border: 1px solid #000;
            padding: 10px;
            display: flex;
        }

        .cust-col {
            width: 50%;
        }

        .cust-col .info-label {
            font-weight: bold;
            display: inline-block;
            width: 110px;
        }

        /* --- TABLE --- */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            flex-grow: 1;
        }

        th,
        td {
            border: 1px solid #000;
            padding: 8px;
            text-align: center;
            vertical-align: top;
        }

        th {
            background-color: #f9f9f9;
            font-weight: bold;
        }

        td:nth-child(2) {
            text-align: left;
        }

        /* --- BOTTOM LAYOUT --- */
        .bottom-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-top: 10px;
            page-break-inside: avoid;
        }

        .notes-section {
            width: 60%;
            font-size: 11px;
        }

        .signature-section {
            width: 35%;
            display: flex;
            justify-content: space-between;
        }

        .sign-box {
            text-align: center;
            width: 45%;
        }

        .sign-line {
            border-top: 1px solid #000;
            margin-top: 50px;
            padding-top: 5px;
            font-weight: bold;
        }
    </style>
</head>

<body>

    <div class="invoice-box">
        <div class="header">
            <img src="<%= logo_url %>" alt="Logo" class="logo">
            <h1 class="company-name">
                <%= business_name %>
            </h1>
        </div>

        <div class="footer">
            <div class="bottom-address">
                <p>
                    <%= landmark %>, <%= street %>, <%= area %>, <%= city %>, <%= country %>
                </p>
                <p>
                    <%= tele_no %>, <%= email %>, <%= website %>
                </p>
            </div>
        </div>

        <h2 style="text-align: center; text-decoration: underline; margin: 10px 0;">DELIVERY CHALLAN</h2>

        <div class="top-info">
            <div class="info-column">
                <div class="info-row"><span class="info-label">DC No:</span> <span>
                        <%= challan_no %>
                    </span></div>
                <div class="info-row"><span class="info-label">Date:</span> <span>
                        <%= challan_date %>
                    </span></div>
            </div>
            <div class="info-column">
                <div class="info-row"><span class="info-label">P.O No:</span> <span>
                        <%= po_no || "---" %>
                    </span></div>
                <div class="info-row"><span class="info-label">P.O Date:</span> <span>
                        <%= po_date || "---" %>
                    </span></div>
            </div>
        </div>

        <div class="customer-section">
            <div class="cust-col">
                <span class="info-label">Receiver:</span>
                <span><strong>
                        <%= customer_name %>
                    </strong></span>
            </div>
            <div class="cust-col">
                <span class="info-label">Delivery Place:</span>
                <span>
                    <%= delivery_place %>
                </span>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th style="width: 5%;">Sr #</th>
                    <th style="width: 35%;">Items</th>
                    <th style="width: 15%;">Range</th>
                    <th style="width: 15%;">Ref</th>
                    <th style="width: 15%;">Design</th>
                    <th style="width: 15%;">Qty</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach((item, index)=> { %>
                    <tr>
                        <td>
                            <%= index + 1 %>
                        </td>
                        <td>
                            <%= item.name %>
                        </td>
                        <td>
                            <%= item.range %>
                        </td>
                        <td>
                            <%= item.ref || "" %>
                        </td>
                        <td>
                            <%= item.design || "As Approved" %>
                        </td>
                        <td>
                            <%= item.quantity %>
                                <%= item.unit %>
                        </td>
                    </tr>
                    <% }); %>
                        <tr>
                            <td colspan="5" style="text-align: right; font-weight: bold;">Total Pcs >>>>>></td>
                            <td style="font-weight: bold;">
                                <%= total_qty %>
                            </td>
                        </tr>
            </tbody>
        </table>

        <div class="bottom-wrapper">
            <div class="notes-section">
                <strong>Note:</strong>
                <ul style="padding-left: 15px; margin-top: 5px;">
                    <li>Goods once delivered shall never be taken back. [cite: 19]</li>
                    <li>Please check the quantity and quality at delivery. [cite: 19]</li>
                    <li>Complaint if any will be entertained within 3 days of delivery. [cite: 20]</li>
                </ul>
            </div>

            <div class="signature-section">
                <div class="sign-box">
                    <div class="sign-line">Delivered by</div>
                </div>
                <div class="sign-box">
                    <div class="sign-line">Received by</div>
                </div>
            </div>
        </div>
    </div>

</body>

</html>