<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= business_name %> Quotation
    </title>
    <style>
        /* --- A4 SETTINGS --- */
        @page {
            size: A4;
            margin: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #E0E0E0;
            font-size: 12px;
            color: #000;
        }

        .invoice-box {
            /* Fixed A4 Dimensions */
            height: 297mm;
            width: 210mm;
            margin: 0 auto;
            padding: 30px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.15);
            box-sizing: border-box;

            /* Flex layout to push footer to bottom */
            display: flex;
            flex-direction: column;
        }

        @media print {
            body {
                background-color: #fff;
            }

            .invoice-box {
                width: 100%;
                height: 297mm;
                margin: 0;
                padding: 30px;
                box-shadow: none;
            }
        }

        /* Prevent shrinking of key sections */
        .header,
        .footer,
        .top-info,
        .customer-section,
        .bottom-wrapper {
            flex-shrink: 0;
        }

        /* --- HEADER & ADDRESS --- */
        .header {
            text-align: center;
            margin-bottom: 5px;
        }

        .logo {
            width: 120px;
            height: auto;
            display: block;
            margin: 0 auto 10px auto;
        }

        .company-name {
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
            margin: 0;
        }

        .footer {
            margin-bottom: 20px;
            font-size: 11px;
        }

        .bottom-address {
            text-align: center;
            padding-top: 5px;
        }

        .bottom-address p {
            margin: 3px 0;
        }

        /* --- TOP INFO GRID --- */
        .top-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            padding: 10px 0;
        }

        .info-column {
            width: 48%;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
        }

        .info-label {
            font-weight: bold;
        }

        .info-row span:last-child {
            text-align: left;
        }

        /* --- CUSTOMER SECTION --- */
        .customer-section {
            margin-bottom: 20px;
            border: 1px solid #000;
            padding: 10px;
            display: flex;
            justify-content: space-between;
        }

        .cust-col-left {
            width: 100%;
        }

        .cust-col-left .info-row {
            display: flex;
            justify-content: flex-start;
            margin-bottom: 5px;
        }

        .cust-col-left .info-label {
            width: 110px;
            min-width: 110px;
            font-weight: bold;
        }

        /* --- TABLE --- */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
            flex-grow: 1;
        }

        th,
        td {
            border: 1px solid #000;
            padding: 8px;
            text-align: center;
            vertical-align: top;
        }

        /* Specific Alignments */
        th:nth-child(2) {
            text-align: center;
        }

        td:nth-child(2) {
            text-align: left;
        }

        th {
            background-color: #f9f9f9;
            height: 40px;
            font-weight: bold;
        }

        /* --- BOTTOM LAYOUT --- */
        .bottom-wrapper {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-top: 10px;
            page-break-inside: avoid;
        }

        .left-section {
            width: 50%;
            font-size: 11px;
        }

        .right-section {
            width: 45%;
        }

        /* Terms */
        .terms h4 {
            margin: 0 0 5px 0;
            text-decoration: underline;
            font-size: 12px;
        }

        .terms ul {
            padding-left: 20px;
            margin: 5px 0;
        }

        .terms li {
            margin-bottom: 5px;
        }

        /* Totals */
        .totals-table {
            width: 100%;
            border-collapse: collapse;
        }

        .totals-table td {
            text-align: right;
            border: 1px solid #000;
        }

        .totals-table .label {
            font-weight: bold;
            text-align: left;
        }

        /* --- SIGNATORY ALIGNMENT --- */
        .signatory {
            margin-top: 30px;
            width: 200px;
            /* Constrain width to align line and text */
            text-align: center;
            /* Center contents within the 200px */
        }

        .signatory img {
            display: block;
            margin: 10px auto 5px auto;
            /* Centers image and adds spacing */
            max-width: 120px;
            height: auto;
            object-fit: contain;
        }

        .sign-line {
            border-top: 1px solid #000;
            width: 100%;
            padding-top: 5px;
            font-weight: bold;
        }

        .sign-details {
            font-size: 11px;
            margin-top: 5px;
        }
    </style>
</head>

<body>

    <div class="invoice-box">
        <div>
            <div class="header">
                <img src="<%= logo_url %>" alt="Logo" class="logo">
                <h1 class="company-name">
                    <%= business_name %>
                </h1>
            </div>

            <div class="footer">
                <div class="bottom-address">
                    <p>
                        <%= landmark %>, <%= street %>, <%= area %>, <%= city %>, <%= country %>
                    </p>
                    <p>
                        <%= tele_no %>, <%= email %>, <%= website %>
                    </p>
                </div>
            </div>

            <h2 style="text-align: center; text-decoration: underline; margin: 10px 0;">QUOTATION</h2>

            <div class="top-info">
                <div class="info-column">
                    <div class="info-row"><span class="info-label">Quote #:</span> <span>
                            <%= invoice_no %>
                        </span></div>
                    <div class="info-row"><span class="info-label">Customer ID:</span> <span>
                            <%= customer_id || "N/A" %>
                        </span></div>
                </div>
                <div class="info-column">
                    <div class="info-row"><span class="info-label">Date:</span> <span>
                            <%= date %>
                        </span></div>
                    <div class="info-row"><span class="info-label">Valid Until:</span> <span>
                            <%= valid_until_date %>
                        </span></div>
                </div>
            </div>

            <div class="customer-section">
                <div class="cust-col-left">
                    <div class="info-row">
                        <span class="info-label">Customer:</span>
                        <span>
                            <strong>
                                <%= customer_name %>
                            </strong><br>
                            <%= customer_landmark %>, <%= customer_street %>, <%= customer_area %><br>
                                        <%= customer_city %>, <%= customer_country %>
                        </span>
                    </div>
                </div>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th style="width: 5%;">Sr #</th>
                    <th style="width: 35%;">Descriptions</th>
                    <th style="width: 10%;">Range</th>
                    <th style="width: 10%;">Reference</th>
                    <th style="width: 10%;">QTY</th>
                    <th style="width: 10%;">Rate</th>
                    <th style="width: 20%;">Amount</th>
                </tr>
            </thead>
            <tbody>
                <% items.forEach((item, index)=> { %>
                    <tr>
                        <td>
                            <%= index + 1 %>
                        </td>
                        <td>
                            <%= item.name %>
                        </td>
                        <td>
                            <%= item.range || "Wooden" %>
                        </td>
                        <td>
                            <%= item.reference || "SS" %>
                        </td>
                        <td>
                            <%= item.quantity %>
                        </td>
                        <td>
                            <%= item.price %>
                        </td>
                        <td>
                            <%= (item.quantity * item.price).toFixed(2) %>
                        </td>
                    </tr>
                    <% }); %>
                        <tr>
                            <td colspan="7" style="height: 100%;"></td>
                        </tr>
            </tbody>
        </table>

        <div class="bottom-wrapper">
            <div class="left-section">
                <div class="terms">
                    <h4>Terms & Conditions</h4>
                    <ul>
                        <li>Payment term 50% advance and 50% at the time of delivery.</li>
                        <li>Complaint will be entertain with 3 days of delivery.</li>
                        <li>Goods once delivered should not be taken back.</li>
                        <li>Delivery Period will be 5 to 6 weeks after confirmation of order.</li>
                        <li>Transportation will paid by the customer.</li>
                    </ul>
                </div>

                <div class="signatory">
                    <div style="margin-bottom: 10px; text-align: left;">Thank You For Your Business</div>
                    <img src="<%= sign_url %>" alt="Stamp" />
                    <div class="sign-line">
                        <%= signatory_name %>
                    </div>
                    <div class="sign-details">
                        <%= signatory_designation %><br>
                            <%= signatory_phone %>
                    </div>
                </div>
            </div>

            <div class="right-section">
                <table class="totals-table">
                    <tr>
                        <td class="label">Sub-Total</td>
                        <td>
                            <%= taxable_value %>
                        </td>
                    </tr>
                    <tr>
                        <td class="label">GST-<%= sales_tax_rate %>%</td>
                        <td>
                            <%= sales_tax %>
                        </td>
                    </tr>
                    <tr>
                        <td class="label" style="font-size: 14px;">G.Total</td>
                        <td style="font-weight: bold; font-size: 14px;">
                            <%= value_including_sales_tax %>
                        </td>
                    </tr>
                </table>
            </div>
        </div>

    </div>

</body>

</html>